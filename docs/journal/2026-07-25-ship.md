---
title: "journal — ship day"
author: "<wtl>"
project: wtl-dllm
tags: [journal, release]
---

# 2026-07-25 — ship

One day, research to tag. The gibberish everyone warned about never showed up; the surprises were subtler and better. The eval design bit back once (digit bands were secretly an extrapolation test), the infill demo exposed that I'd never actually trained operand positions, and the fix for each made the results stronger than the plan promised.

Ship audits: headers lint clean after teaching the linter that svelte files lead with an html comment; the humanize pass caught a negative-parallelism habit in my own prose (five instances, all rewritten); the commit log reads in one voice with zero attribution noise; run.ps1 cold-launches server and UI and finds the newest checkpoint on its own.

What ships: seven trained models (three L1 arms, mix, L2 pair, L3 pair), a results ledger where every number carries its config, three passed gates, and a UI that shows a 10M-parameter model condensing arithmetic out of noise at 5ms a step. v0.1.0.
